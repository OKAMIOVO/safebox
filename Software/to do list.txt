备用完全完成，包括休眠唤醒
先完成按键板，再完成主板

主系统和备用系统通信已完成

主系统
先完成 主板，然后用电脑串口模拟触摸板上报事件
需要定协议
触摸与机械按键板相同
指纹上报 指纹状态
但是接收语音的回复电脑不大好做

触摸板先完成接收语音信息的回复

触摸板语音播报，触摸，指纹底层驱动通过移植指纹锁的代码，快速完成


以下不属于本项目
异步通信接口标准化，完成后所有4个板子共用通信部分代码，底层完全复用，并且要做到通过裁剪适用异步窗口以外的所有串口通信，
上层共用一份源码，通过宏定义区分主板/按键板，主系统/备用系统

底层串口部分
接收
软件FIFO，用定时器辅助提供帧中断

发送接口，静态数组，发送个数，发送完成标志位清零

发送对列入队，队列可以采用发送buf作为队列元素而不是 数据帧结构体（命令，长度，buf）
停等实现要发送者自己解析发送buf，对比接收数据，是否出队

设备列表
函数指针状态机 

UB5H电机驱动板改用串口通信，电源板与电机驱动板接口改用模拟串口

电脑模拟通信板，方便调试




